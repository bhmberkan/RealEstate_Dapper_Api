@model List<ResultProductDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}



<div class="w3l-news" id="news">
    <section id="grids5-block" class="py-5">
        <div class="container py-lg-5 py-md-4 py-2">
            <h3 class="title-big text-center">Son 10 İlanımız </h3>
            <div class="row mt-lg-5 mt-4 pt-3">
                @foreach (var item in Model.OrderByDescending(x => x.productId).Take(10))
                {
                    <div class="col-lg-4 col-md-6 mb-5">
                        <div class="grids5-info">
                            <span class="posted-date">
                                <a href="#blog-single">



                                    <span class="small">@item.AdvertisementDate.ToString("yyyy")</span>
                                    <span class="big">@item.AdvertisementDate.ToString("dd")</span>
                                    <span class="small">@item.AdvertisementDate.ToString("MMMM")</span>
                                </a>
                            </span>
                            <a href="#blog-single" class="d-block zoom">
                                <img src="@item.coverimage" alt="" class="img-fluid news-image" style="height:250px;" />
                            </a>
                            <div class="blog-info">
                                <a href="#category" class="category">@item.categoryName</a>
                                <h4><a href="#blog-single">@item.city / @item.district</a></h4>
                                <p>
                                    @(string.IsNullOrEmpty(item.description)
                                        ? ""
                                        : item.description.Length > 85
                                        ? item.description.Substring(0, 85) + "..."
                                        : item.description)
                                    <a href="#">Devamını Gör</a>
                                </p>
                            </div>
                        </div>
                    </div>
                    <br/>
                }



            </div>
        </div>
    </section>
</div>